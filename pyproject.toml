[project]
name = "pix-fraud-realtime"
version = "1.0.0"
description = "Real-time PIX fraud detection platform with Kafka, TimescaleDB and LSTM scoring"
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.black]
line-length = 100
